<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="it.giccisw.demo.introscreen">

    <uses-permission android:name="android.permission.CALL_PHONE"/>
    <uses-permission android:name="android.permission.READ_SMS"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.CAMERA"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name="it.giccisw.demo.introscreen.SplashActivity"
            android:theme="@style/SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name="it.giccisw.demo.introscreen.MainActivity"/>

        <activity
            android:name="it.giccisw.demo.introscreen.IntroActivity"
            android:theme="@style/Theme.MaterialIntro"/>

    </application>

</manifest>
